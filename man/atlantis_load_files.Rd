% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlantis.R
\name{atlantis_load_files}
\alias{atlantis_load_files}
\title{Load Atlantis files into model object}
\usage{
atlantis_load_files(atlantis, files, filenames = basename(files))
}
\arguments{
\item{atlantis}{An existing Atlantis S7 object to update with new data.}

\item{files}{Either:
\itemize{
\item Character vector of file paths to Atlantis files
\item An \code{atlantis_file} object (created by \code{read_atlantis_files()})
}

Supported file types include:
\itemize{
\item \code{.geometry} - Box Geometry Model files
\item \code{.prm} - Parameter files (bio and run)
\item \code{.nc} - NetCDF output files
\item \code{.txt}, \code{.csv} - Diet and biomass snapshot files
}}

\item{filenames}{file basenames (used as hints)}
}
\value{
Updated Atlantis S7 object with newly loaded data merged with
existing properties.
}
\description{
Load Atlantis input and output files into an existing Atlantis object. This
function automatically detects file types and assigns data to the appropriate
properties based on file extensions and content. Only properties with newly
loaded data are updated; existing properties are preserved unless overwritten.
}
\examples{
\dontrun{
# Create empty object and load files
atlantis <- new_atlantis()  |>
 atlantis_load_files(atlantis, c(
  "model.geometry",
  "biology.prm",
  "run.prm",
  "output.nc"
))

# Load additional files later
atlantis <- atlantis_load_files(atlantis, "diet_check.txt")
}

}
\seealso{
\code{\link[=read_atlantis_files]{read_atlantis_files()}}, \code{\link[=new_atlantis]{new_atlantis()}}
}
