% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlantis_methods-calibrate.R
\name{inverse_transform_parameter_value}
\alias{inverse_transform_parameter_value}
\title{Map a parameter value from the file scale back to the calibration scale}
\usage{
inverse_transform_parameter_value(x, fun)
}
\arguments{
\item{x}{A numeric vector of parameter values on the file scale, or \code{NULL}.}

\item{fun}{A string naming the transformation used during calibration. One
of \code{"identity"}, \code{"pow10"} (inverse is \code{log10(x)}), \code{"pow2"} (inverse is
\code{log2(x)}) or \code{"exp"} (inverse is \code{log(x)}).}
}
\value{
The values mapped back to the calibration scale, or \code{NULL} if \code{x}
is \code{NULL}.
}
\description{
Inverse of \code{\link[=transform_parameter_value]{transform_parameter_value()}}, used to get starting
values in the space explored by the optimizer.
}
\examples{
inverse_transform_parameter_value(100, "pow10")
inverse_transform_parameter_value(c(2, 4, 8), "pow2")
inverse_transform_parameter_value(0.5, "identity")

}
