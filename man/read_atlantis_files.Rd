% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{read_atlantis_files}
\alias{read_atlantis_files}
\alias{read_bgm}
\alias{read_nc}
\alias{read_group_file}
\alias{read_prm_files}
\alias{read_txt_files}
\alias{read_xml}
\title{Read Atlantis inputs and outputs files}
\usage{
read_atlantis_files(files, filenames = basename(files))

read_bgm(path)

read_nc(path)

read_group_file(path)

read_prm_files(path)

read_txt_files(path, filename)

read_xml(path)
}
\arguments{
\item{files}{vector of paths that point to file(s) to read.}

\item{filenames}{file basenames. This is important for the Shiny app as
files uploaded are renamed but the basename remains available.}

\item{path}{path that points to the file to read.}

\item{filename}{File basename.}
}
\value{
Returns an object of class \code{atlantis_file}, which is a list of
3 elements:
\itemize{
\item \code{path}: the path to the file;
\item \code{type}: the type of input or output;
\item \code{object}: the R object used by the Shiny App.
}
}
\description{
Functions to read Atlantis inputs and outputs.
}
\details{
The \code{read_atlantis_files()} function implements a file processing pipeline
for handling diverse Atlantis ecosystem model file formats. One or several
files can be processed at once. Internally, the way files are processed is
determined by file extension using a switch statement:
\itemize{
\item \code{.bgm}: Box geometry files → \verb{[read_bgm()]}
\item \code{.nc}: NetCDF files → \verb{[read_nc()]}
\item \code{.prm}: Parameter files → \verb{[read_prm_files()]}
\item \code{.csv}: Group definition files → \verb{[read_group_file()]}
\item \code{.txt}: Various output formats → \verb{[read_txt_files()]}
Basic examination of the content is done to determine the type of file and
exhaustive validation is carried out if available.
Note that unknown file types are marked as \code{"unknown"} rather than causing
crashes, and unreadable files are marked as \code{"cannot read"}.
}
}
\section{Functions}{
\itemize{
\item \code{read_bgm()}: Read BGM (box geometry) files

\item \code{read_nc()}: Read NetCDF files and classify as init or main output

\item \code{read_group_file()}: Read CSV group definition files

\item \code{read_prm_files()}: Read and classify PRM parameter files

\item \code{read_txt_files()}: Read and classify TXT output files. The name
of the file is used as a clue to determine the type of files.

\item \code{read_xml()}: Read XML files and convert them to lists

}}
\examples{
hh <- system.file("examples", "inputs", "tiny_biol.prm", package = "atlantio") |>
  read_atlantis_files()

}
