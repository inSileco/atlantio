% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlantis_methods-calibrate.R
\name{transform_parameter_value}
\alias{transform_parameter_value}
\title{Transform a parameter value}
\usage{
transform_parameter_value(x, fun)
}
\arguments{
\item{x}{A numeric vector of parameter values, or \code{NULL}.}

\item{fun}{A string naming the transformation to apply. One of
\code{"identity"}, \code{"pow10"} (\code{10^x}), \code{"pow2"} (\code{2^x}) or \code{"exp"} (\code{exp(x)}).}
}
\value{
The transformed values, or \code{NULL} if \code{x} is \code{NULL}.
}
\description{
Apply a transformation function to a parameter value. This is used to
map calibrated values back to their natural scale, e.g. when a parameter
is calibrated on a log scale.
}
\examples{
transform_parameter_value(2, "pow10")
transform_parameter_value(c(1, 2, 3), "pow2")
transform_parameter_value(0.5, "identity")

}
